apiVersion: v1
kind: Service
metadata:
  name: banking-service
  labels:
    app.kubernetes.io/name: banking-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: banking-service
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
